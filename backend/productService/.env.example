# Copy to .env (same values as auth-service for shared resources)
PORT=3001
HOST=0.0.0.0
DATABASE_URL="postgresql://silver_shop_user:postgres@localhost:5433/silver_shop"
JWT_SECRET="change-me-use-the-same-secret-as-auth-service"
